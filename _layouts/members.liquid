---
layout: default
---
{% if page.title %}
  <div class="members-header">
    <span class="title">{{ page.title }}</span>
    <span class="description">{{ page.description }}</span>
  </div>
{% endif %}
<div class="members-list">
  {% for part in site.data.members %}
    <div class="group">
      <div class="group-header">
        <span class="group-title">{{ part.title }}</span>
        <hr class="group-hr">
      </div>
      <div class="group-member-card-display">
        {% for member in part.group %}
          <div class="group-member-card">
            <div class="member-label">
              <ul class="member-label">
                {% for label in member.label %}
                  <li class="key-value">
                    <span class="key">{{ label.key }}:</span>
                    <span class="value">{{ label.value }}</span>
                  </li>
                {% endfor %}
              </ul>
            </div>
            <!-- 左侧头像+姓名 -->
            {% comment %} <div class="member-name-img">
              <img src="{{ member.img }}" class="member-image" alt="{{ member.name }}">
              <span class="member-name">{{ member.name }}</span>
            </div> {% endcomment %}

            <!-- 右侧固定信息 -->
            {% comment %} <ul class="member-info">
              <li>
                <span class="key"></span>
                <span class="value">{{ member.introduction | default: 'N/A' }}</span>
              </li>
              <li>
                <span class="key">email:</span>
                <span class="value">{{ member.email | default: 'N/A' }}</span>
              </li>
            </ul> {% endcomment %}
          </div>
        {% endfor %}
      </div>
    </div>
  {% endfor %}
</div>
